# EWS Annual Report Extraction Agent - Dependencies

# PDF table extraction (deterministic, like Excel's "Get Data from PDF")
pdfplumber>=0.10.0

# Excel file reading/writing
openpyxl>=3.1.0

# Data manipulation
pandas>=2.0.0

# Fuzzy string matching for data mapping
rapidfuzz>=3.0.0

# LLM integration (for fallback classification/mapping only)
httpx>=0.24.0
langchain-openai>=0.1.0
langchain-core>=0.2.0

# CLI table display
tabulate>=0.9.0

# FastAPI application (REST API for multi-PDF extraction)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.2.0

# RAG pipeline dependencies (isolated rag_agent module)
# ChromaDB includes default ONNX-based embeddings (all-MiniLM-L6-v2) — no PyTorch needed
chromadb>=0.4.22
PyMuPDF>=1.23.0
langchain-community>=0.0.20
